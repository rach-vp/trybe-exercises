// Varrer o array imprimindo seu conteúdo

let numbers = [5, 9, 3, 19, 70, 8, 100, 2, 35, 27];

for (let i in numbers) {
  console.log(`O valor da posição ${i} é ${numbers[i]}`);
}

// Lembrar que no usa da template string o conteúdo deve estar entre crazes (`) e não aspas simples (')
